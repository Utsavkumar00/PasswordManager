# Copy this file to application-local.properties in the project root (same folder as pom.xml).
# That file is gitignored — use it for local MySQL credentials instead of committing secrets.
#
# spring.datasource.url=jdbc:mysql://localhost:3306/password_manager?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# spring.datasource.username=root
# spring.datasource.password=your-mysql-password
